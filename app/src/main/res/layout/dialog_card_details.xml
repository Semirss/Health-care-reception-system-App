<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Patient ID -->
    <TextView
        android:id="@+id/tv_patient_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Patient ID: "
        android:textSize="16sp"
        android:paddingBottom="8dp" />

    <!-- Name -->
    <TextView
        android:id="@+id/tv_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Name: "
        android:textSize="16sp"
        android:paddingBottom="8dp" />

    <!-- Date -->
    <TextView
        android:id="@+id/tv_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Date: "
        android:textSize="16sp"
        android:paddingBottom="8dp" />

    <!-- Current History -->
    <TextView
        android:id="@+id/tv_history_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Medical History:"
        android:textSize="16sp"
        android:paddingBottom="8dp" />

    <TextView
        android:id="@+id/tv_history"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No history available"
        android:textSize="14sp"
        android:paddingBottom="16dp" />

    <!-- Input for New Findings -->
    <EditText
        android:id="@+id/et_new_findings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter new findings"
        android:inputType="textMultiLine"
        android:minHeight="100dp"
        android:padding="8dp" />

    <!-- Input for Prescribed -->
    <EditText
        android:id="@+id/et_prescribed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter prescribed treatment"
        android:inputType="textMultiLine"
        android:minHeight="100dp"
        android:padding="8dp"
        android:layout_marginTop="8dp" />

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/btn_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/btn_update"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Update History" />
    </LinearLayout>
</LinearLayout>